#!/data/data/com.termux/files/usr/bin/sh
cd /data/data/com.termux/files/usr/bin
rm gcc
rm g++
rm cpp
rm gfortran >/dev/null 2>&1 
ln -s clang gcc
ln -s clang g++
ln -s clang cpp
ln -sf gcc cc
ln -sf g++ c++
echo "now using clang as default compiler"
